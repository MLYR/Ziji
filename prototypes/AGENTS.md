# Ziji Prototypes — 原型局部规范

- 开始原型、视觉生成或交互调整前完整读取本目录 `README.md`，以其任务条件、工具选择、候选落点、评审和基线晋升规则为准；根 `AGENTS.md` 的产品范围、账务和客户端边界继续生效。
- Codex 内新建或重做可点击 HTML 原型使用 `huashu-design` skill；设计规范检索可辅以 `ui-ux-pro-max`。Skill 负责通用生成流程，本目录文档只负责 Ziji 的事实源和落点。
- AI 生成稿只是评审材料，不是生产代码、业务规则、无障碍证据或验收结论；不得直接复制到 `web/` 或 `mobile/`，不得从演示数据反推账务、权限或同步规则。
