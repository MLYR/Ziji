# B3 验收收口核对

- 关联任务：BUG-B3-001；本记录不替代 QA-B3-001 的端到端验收。
- 核对日期：2026-09-05。
- 代码基线：`ae3372eca74e38e298b6b3c44a1e2f5dc9a062ed`，开始核对时工作树干净。
- 结论：B3 原有 32 项任务记录为 DONE，但尚不能证明 B3 全部验收完成。保留原任务历史状态，新增收口任务跟踪缺口；INV-001～018 保持“已实现”，不提升为“已验收”。
- 本次没有运行 Java 编译/测试、前端启动/构建、Web/原生 E2E、真实供应商冒烟或远程 CI，没有取得新的独立审查结论。

## 0. 2026-09-07 增量验收证据

本轮未把历史 `DONE` 记录自动提升为 B3 最终验收，新增证据如下：

| 检查 | 结果 | 说明 |
| --- | --- | --- |
| Backend B3 定向集合 | 55 例运行、0 失败、0 错误、1 跳过 | 跳过项是未设置 `ZIJI_THS_SMOKE_ENABLED` 的真实供应商门控；其余覆盖持仓顺序回归、金标准、原子性、并发、幂等、权限、收益日历、投影重建、THS 适配器与合规测试 |
| 真实同花顺冒烟 | 1/1 通过 | `ZIJI_THS_SMOKE_ENABLED=true`；股票 000001、ETF 510300、基金 005827 均按 CLOSE/UNIT_NAV 正确落库 |
| Web 静态与生产构建 | `check`、`build` 通过 | 仅保留既有 Fast Refresh 和 chunk size warning |
| Web 真实 Backend proxy E2E | 5/5 通过 | B1 2/2、B3 投资闭环 1/1、Vite proxy 2/2；B3 覆盖买入、卖出、分红、Dashboard、全部/单标收益日历与日期明细 |
| Mobile 静态与单测 | `check` 通过；20 suites/174 tests 通过 | 仍是模拟/屏幕边界验证，不替代原生端到端 |
| Mobile Maestro | 未执行 | `adb devices -l` 当前为 0 台设备；iOS 模拟器虽可用但尚未安装并运行可验收应用包 |

本轮同时修复 `BUG-MD-002`（同花顺重试先等待本地限流许可）并补充同一 `tradeAt` 下按 `recordedAt` 重建持仓的回归。`T-GATE-001` 书面授权门禁已于 2026-09-18 凭用户书面风险接受记录闭合（合规评估 §6）。

**2026-09-18 后续更新（独立复核与批次门禁）：** 独立 R3 复核已取得，结论为**「不通过」**（2 阻塞 + 7 重要 + 7 次要），详见 `doc/status/R3-B3-独立复核.md`；两个阻塞项登记为 `BUG-B3-002`、`BUG-B3-003`，修复后须重新独立复核。Web 真实 Backend proxy E2E 已由 `nightly-batch.yml` 稳定产出：`run 35369619204`（SHA `5f441d3`）三个作业全通过，其中 Web E2E 5/5、Backend 全量测试 4m40s 通过。因此本文件原先「独立 R3 复核」「iOS/Android 原生 E2E」两项缺口的描述以下表为准。当前**仍未闭合**：Mobile iOS/Android 投资原生 E2E（`.maestro/investment.yaml` 已提交但未实跑）、两个阻塞项。B3/RTM 最终验收状态保持不变。

## 1. 当前可复核证据

| 检查 | 本次结果 | 证据边界 |
| --- | --- | --- |
| 台账叶子任务重算 | 原 318 项：220 DONE、98 BACKLOG；E10=97/97，E30=32/32 | 按状态列识别任务，允许缺尾部竖线的真实行；不使用固定 11 列过滤；新增 BUG-B3-001 后为 220/319、E30=32/33 |
| `rtk proxy pnpm --filter web test` | 11 files、55 tests 全通过 | 当前执行；有 jsdom canvas getContext 未实现警告；不证明图表视觉或浏览器 E2E |
| Mobile 投资三个测试文件 | 3 suites、8 tests 全通过 | 当前执行；仅屏幕/API client 模拟边界，不等于 iOS/Android 原生流程 |
| 本地 Surefire XML | 投资金标准、权限、幂等、失败原子性、并发、投影重建、日历、绩效、行情同步等报告无失败 | 既存产物，非本轮执行；未取得绑定当前 SHA 的执行元数据，不汇总成当前代码全量通过 |
| 同花顺真实冒烟 XML | `tests=1, skipped=1, failures=0, errors=0` | `backend/target/surefire-reports/TEST-app.ziji.ThsRealSmokePostgresIntegrationTests.xml`；不能佐证历史“股票/ETF/基金全部落库” |
| Android 设备 | `adb devices -l` 列表为空 | Maestro/adb 已安装，但当前无连接设备；查询启动了本地 adb daemon，未启动应用 |
| iOS 模拟器 | iOS 26.5 模拟器可用，均为 Shutdown | 不再沿用“无原生环境”的旧假设；尚未验证安装包、后端连接与支持版本 |
| 独立角色复核 | **已取得：结论「不通过」**（2026-09-18，基线 `9c8f2af`），2 阻塞 + 7 重要 + 7 次要，见 `doc/status/R3-B3-独立复核.md` | 阻塞项：投资成交无修订/作废路径；QA-INV-002 证据空转。已登记 `BUG-B3-002`/`BUG-B3-003`，修复后需重新复核 |

Mobile 定向命令（执行时 package script 已带 `--runInBand`，结果确实只匹配以下三个文件）：

```sh
rtk proxy pnpm --filter mobile test -- --runTestsByPath src/investments/investment-screen.test.tsx src/investments/investment-detail-screen.test.tsx src/investments/investment-api-client.test.tsx
```

## 2. 原有 32 项任务的收口映射

“有历史报告”与“当前通过”分别记录；以下不重新授予原任务 DONE 或产品验收结论。

| 任务范围 | 验收对象 | 当前核对结论 / 尚缺证明 |
| --- | --- | --- |
| CHG-MD-001、EXT-TS-001 | 数据源变更和风险确认 | PRD/RTM/合规记录已切同花顺；T-GATE-001 冲突已于 2026-09-18 凭用户书面风险接受记录闭合 |
| BE-INV-001、BE-TS-001 | 产品模型、来源映射、手工兜底 | `MarketDataRemoteSearchTests` 既存 5/5；同花顺无在线搜索，验收应按已确认的手工创建+代码映射范围 |
| BE-TS-002、BE-TS-003、QA-TS-001 | 日线/净值、增量、限流配额、降级 | 既存 Sync PostgreSQL 4/4、Sync service 7/7、THS adapter 8/8；需保持用例到当前执行版本的追踪 |
| BE-TS-004 | 手工价格与修订优先级 | 日历 revision 报告 7/7；需对照 T-INV-010 的原值、原因和审计链，不用文件名代替覆盖审计 |
| QA-TS-002 | 股票、ETF、基金真实冒烟 | 当前报告 skipped；源码只要求至少一个产品成功，零价格且临时故障会直接 return，部分失败只告警；绿色结果也不足以证明三类全覆盖 |
| SEC-TS-001 | 无凭据和日志边界 | `ThsComplianceTests` 既存 4/4；不等于实际客户端包、运行日志和指标全量扫描 |
| BE-INV-002、BE-INV-005、BE-INV-006 | 券商现金、成本排除、估值和收益 | 既存成本排除 1/1、金标准 2/2；跨币种仍属 B4，B3 同币种验证不能证明 V1 跨币种完成 |
| BE-INV-003、BE-INV-004、BE-INV-008 | 买卖分红、原子性、持仓与 API | 既存权限 6/6、回滚 6/6、幂等 3/3、并发 1/1、领域 7/7、属性 3/3；完整 R3 独立结果待取得可追溯证据 |
| BE-INV-007 | 年化和 XIRR 失败语义 | 既存 AnnualizedReturn 2/2、MVC 2/2；需绑定执行版本 |
| BE-INV-009、BE-INV-010、QA-INV-003 | 日历、日期状态、修订、重建与 API | 既存 revision 7/7；尚缺客户端全部/单标、日期明细的一致性端到端证据 |
| QA-INV-001、QA-INV-002 | 金标准、随机序列与重建 | 既存金标准 2/2、属性 3/3、持仓重建 1/1；固定种子与真实边界证据仍须纳入最终归档 |
| BE-DASH-004 | Dashboard 投资一致性 | 既存 HTTP PostgreSQL 2/2；RTM DASH-009 仍“已设计”，需覆盖 T-DASH-009 全条件后再同步，不机械修改 |
| WEB-INV-001～004 | Web 投资流程和日历 | 本轮单测 55/55；现有 B3 E2E 创建账户/产品/价格、买入、市值和 Dashboard 断言，但无卖出/分红、单标切换、日期明细和实际键盘/读屏验证 |
| MOB-INV-001～003 | Mobile 投资流程和日历 | 本轮投资定向 8/8；`.maestro/smoke.yaml` 只检查登录页和主题，不提交认证，无投资 E2E |
| QA-B3-001、DOC-B3-001 | 三端回归、RTM 与门禁同步 | 必须先补上述证据；DOC 的“仅剩两项外部阻塞”是旧快照，不代表当前剩余事项 |

## 3. 基线冲突与决策边界

1. 根 `AGENTS.md` §3/§4 已按用户 2026-09-07 决策同步为同花顺；系统架构 ADR-005、领域来源示例和任务台账当前口径同步更新。历史记录中的 Tushare 仅表示当时的实现或验证事实。
2. **已闭合（2026-09-18）：** `doc/测试与验收方案.md` T-GATE-001“有书面授权记录，否则阻塞上线”与 §16.5、合规评估的口径冲突，由用户选择风险接受口径闭合：合规评估 §6 新增用户书面风险接受记录，T-GATE-001 行同步标注以该记录为满足条件。公开、免费、无 key 不等于授权的事实判断不变，门禁本身未削弱，发布前复查义务保留。
3. `doc/测试与验收方案.md` §16.6 是 2026-09-03 历史快照，部分缺口已被后续测试覆盖；既不能全部当作现存开发缺陷，也不能凭后续台账声明全部关闭。

## 4. 完成 BUG-B3-001 的必要证据

| 待完成项 | 通过条件 | 当前约束 |
| --- | --- | --- |
| ~~基线冲突闭合~~ | 已于 2026-09-18 凭用户书面风险接受记录闭合（合规评估 §6、T-GATE-001 行已同步） | 完成 |
| Web 投资端到端 | 真实后端下覆盖买入、卖出、分红、估值、全部/单标日历、日期明细和 Dashboard；记录 SHA、命令、环境、结果 | `nightly-batch.yml` 的 `web-proxy-e2e` 作业自 2026-09-18 起提供固定产出（Runner 环境、SHA、报告制品）；需先确认其首轮运行结果 |
| iOS/Android 投资端到端 | 两平台真实应用与后端验证关键流程、日期状态、网络失败提示及可访问性；保存平台、构建版本和报告 | 无运行设备/应用证据；不得用 Jest 代替，也不自行构建启动 |
| 供应商三类冒烟 | 三类均有正确来源/日期/价格类型的真实入库结果；短时故障明确记为未完成而非验收通过 | 当前 Java 不编译测试约束；先纠正测试判定与补回归，再经授权执行或取外部报告 |
| R3 与需求追踪 | 取得实际独立审查结果，逐条追踪 INV-001～018、DASH-009 的测试与执行证据；只对确实满足条件的需求更新状态 | **独立审查已取得但结论为「不通过」**：投资成交无修订/作废路径（§4.2-3 事实链在 B3 不成立）、QA-INV-002 建立在死表 `position_snapshots` 上断言恒真。见 `doc/status/R3-B3-独立复核.md`；两项阻塞修复并重新复核后方可收口 |

全部条件满足后再收口任务、重算台账和更新验收记录。B2、B4 和 V1 发布门禁不因本次 B3 收口自动完成。
