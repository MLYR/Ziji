{
  "name": "ziji",
  "version": "0.1.0",
  "private": true,
  "description": "资迹 Ziji 个人与家庭财务管理项目",
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": "22.22.3",
    "pnpm": "10.4.1"
  },
  "scripts": {
    "api:lint": "redocly lint openapi/ziji-v1.yaml",
    "api:check": "pnpm api:lint && node scripts/check-openapi.mjs && pnpm api:contract:test",
    "api:contract:test": "node scripts/check-transaction-contract.mjs && node scripts/check-account-creation-contract.mjs && node scripts/check-liability-contract.mjs && node scripts/check-transaction-write-contract.mjs && node scripts/check-account-archive-contract.mjs",
    "api:breaking": "node scripts/check-openapi-breaking.mjs",
    "api:generate": "pnpm --filter @ziji/api-types run generate",
    "api:types:check": "pnpm --filter @ziji/api-types run check",
    "check:workspace": "pnpm --recursive --if-present run check",
    "security:dependencies": "scripts/security-scan.sh dependencies",
    "security:licenses": "scripts/security-scan.sh licenses",
    "security:secrets": "scripts/security-scan.sh secrets",
    "security:scan": "scripts/security-scan.sh all",
    "test:workspace": "pnpm --recursive --if-present run test"
  },
  "pnpm": {
    "overrides": {
      "image-size": "npm:image-size-next@1.2.2"
    }
  },
  "devDependencies": {
    "@redocly/cli": "2.46.1"
  }
}
